知识点：

// 单链表节点类
class ListNode {
    int val;        // 节点存储的值
    ListNode next;  // 指向下一个节点的指针
    
    // 构造函数
    ListNode(int val) {
        this.val = val;
        this.next = null;
    }
}


让长的链表先走差值，然后在一块走，相等的时候就是那个点


或者是双指针，遍历链表，比如a指针遍历链表a，b指针遍历链表b，那么遍历完以后再遍历 对方的，相等的点就是相交的点
